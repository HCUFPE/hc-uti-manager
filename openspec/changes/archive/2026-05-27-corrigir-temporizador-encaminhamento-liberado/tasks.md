## 1. Implementação no Frontend

- [x] 1.1 Atualizar o componente `frontend/src/components/BedCard.vue` para exibir o temporizador apenas se `!encaminhamentoLiberado`
- [x] 1.2 Adicionar a etiqueta "Encaminhamento Liberado" no componente `frontend/src/components/BedCard.vue` quando `encaminhamentoLiberado` for true

## 2. Testes e Validação

- [x] 2.1 Compilar o frontend com `npm run build` para validar e garantir a inexistência de erros de tipos ou compilação
