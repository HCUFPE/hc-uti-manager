## 1. Ajustes no Backend

- [x] 1.1 Modificar `src/routers/auth.py` para normalizar `user["username"]` para minúsculas imediatamente após obter o retorno de `auth_handler.authenticate_user`.

## 2. Testes e Publicação

- [ ] 2.1 Commit, push e deploy seguro com rebuild na VM usando `git_pull_and_rebuild.py` (sem tocar no banco de dados).
