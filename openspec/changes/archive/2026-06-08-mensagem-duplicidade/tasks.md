## 1. Implementação

- [x] 1.1 Atualizar a mensagem de erro da `HTTPException` de duplicidade no backend para `"Solicitação para este prontuário já inserida."`.
- [x] 1.2 Atualizar o frontend em `frontend/src/views/Solicitacoes.vue` para exibir `error.response?.data?.detail` na notificação da função `salvarNova`.

## 2. Validação e Testes

- [x] 2.1 Atualizar os testes locais e executá-los para verificar que a validação retorna a mensagem de erro correta.
