## 1. Verificação e Remoção do Mock

- [x] 1.1 Inspecionar a lógica de conflito em `src/controllers/leitos_controller.py` para atestar a corretude do sistema de detecção de conflitos
- [x] 1.2 Remover a reserva mock do próximo paciente no leito `UTI-03` no array `mock_beds` em `src/controllers/leitos_controller.py`

## 2. Testes e Validação

- [x] 2.1 Reiniciar/verificar o servidor backend para garantir a aplicação das alterações sem erros
- [x] 2.2 Validar a nova estrutura com a compilação do frontend (`npm run build`)
