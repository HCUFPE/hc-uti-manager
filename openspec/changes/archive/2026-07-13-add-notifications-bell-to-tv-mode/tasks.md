## 1. Implementação no Frontend

- [x] 1.1 Importar e adicionar o componente `NotificationsPopover` em `frontend/src/views/Home.vue` na linha de controles, visível quando `uiStore.isTvMode` for verdadeiro.
- [x] 1.2 Importar e adicionar o componente `NotificationsPopover` em `frontend/src/layouts/DefaultLayout.vue` ao lado do `ProfileDropdown` para o modo normal.

## 2. Validação Local

- [x] 2.1 Compilar o projeto e validar localmente o funcionamento e renderização do popover de notificações.
