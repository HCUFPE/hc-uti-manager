## Context

O sininho deve ser exibido exclusivamente no Modo TV.

## Goals / Non-Goals

**Goals:**
- Remover o componente `<NotificationsPopover />` do cabeçalho em `DefaultLayout.vue`.
- Remover a importação correspondente em `DefaultLayout.vue` para manter o código limpo.

**Non-Goals:**
- Alterar o `Home.vue` (manter a exibição no Modo TV).
