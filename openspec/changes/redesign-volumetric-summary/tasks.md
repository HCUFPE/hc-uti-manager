## 1. Implementação no Backend

- [x] 1.1 Atualizar `src/providers/implementations/indicadores_provider.py` para calcular e retornar as novas chaves do ciclo de vida das solicitações (`concluidas`, `canceladas`, `reservas_ativas`, `pendentes_fila`, etc).

## 2. Implementação no Frontend

- [x] 2.1 Atualizar `frontend/src/views/Indicadores.vue` para dividir o bloco de volumes em duas tabelas/quadros distintos.

## 3. Validação

- [ ] 3.1 Validar se as somas do ciclo de vida fecham perfeitamente no dashboard de indicadores.
